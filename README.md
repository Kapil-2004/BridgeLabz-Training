## Day 1 – Leet Code Codebase (16 Dec)

On **16 Dec (Day 1)**, I completed **5 LeetCode-style problems** in this `leet-code-codebase` folder using **Java**:

- **`Hello.java`**: Prints a simple "Hello World" message to verify the setup.
- **`AreaSquare.java`**: Calculates the **area of a square** from a given side length.
- **`Palindrome.java`**: Checks whether a given string is a **palindrome**, ignoring non-alphanumeric characters and case (similar to a classic LeetCode palindrome problem).
- **`RemoveDuplicate.java`**: Removes **duplicate integers** from an array using a set and returns the unique values.
- **`TwoSum.java`**: Implements the classic **Two Sum** problem using a map to find two indices whose elements add up to a target.

These files together represent my **Day 1 progress** in Java, practicing problem-solving and basic data structure usage for interview-style questions.



